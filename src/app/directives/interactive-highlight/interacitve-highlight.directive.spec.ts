import { InteracitveHighlightDirective } from './interacitve-highlight.directive';

describe('InteracitveHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new InteracitveHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
